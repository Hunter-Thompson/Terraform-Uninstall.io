---
 apiVersion: v1
 kind: Pod
 metadata:
   name: demo-pod
   labels:
     app: web
 spec:
   containers:
     - name: container1
       image: nginx
     - name: container2
       image: nginx
       ports:
         - containerPort: 6739
